# 分享管理

Cdifit 支持创建和管理文件分享链接，包括私有分享、公开分享和付费分享。

## 分享权限

分享权限使用 boolset 编码，支持以下权限：

| 权限 | 说明 |
|-----|------|
| `anonymous` | 匿名用户权限 |
| `everyone` | 所有用户权限 |
| `same_group` | 同组用户权限 |
| `other` | 其他组用户权限 |

## 权限编码

权限值为 base64 编码的 boolset，常见值：
- `AQ==` - 仅读取权限
- `BQ==` - 读取和下载权限